{
    "require": {
        "bcosca/fatfree": "*",
        "dompdf/dompdf": "0.6.*",
        "vlucas/valitron": "^1.4"
    },
    "scripts": {
        "run" : ["php -S localhost:8080 -t ."]
    },
    "config": {
        "process-timeout":0
    }
}
